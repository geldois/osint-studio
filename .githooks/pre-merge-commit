#!/bin/sh
command -v pnpm >/dev/null 2>&1 || {
    echo "osint-studio: 'pnpm' not found on PATH — activate mise (see README: Setup), or bypass once with 'git merge --no-verify'." >&2
    exit 1
}
exec scripts/run precommit
